# sft_distill/prompts.py

DEFAULT_GENERATION_PROMPT = """你是一位专业的数据生成专家。请基于以下领域知识，生成一条高质量的指令-回应对用于大语言模型微调。

要求：
1. 指令必须清晰、具体、可执行
2. 输出必须基于提供的知识，准确、详细、有逻辑
3. 覆盖知识的多个维度（概念解释、原理分析、应用示例、注意事项等）
4. 避免简单复述原文，要有适当的推理和总结

知识内容：
{content}

请生成一个JSON格式的结果：
{{
    "instruction": "明确的任务指令",
    "input": "任务的输入（如果需要）",
    "output": "高质量的详细回答"
}}

注意：只返回纯JSON，不要包含任何其他文本。"""

DEFAULT_EVAL_PROMPT = """作为数据质量评估专家，请对以下合成数据进行多维度评分。

待评估数据：
指令：{instruction}
输入：{input}
输出：{output}

请从以下维度评分（每项1-10分）：
1. 准确性：回答是否正确、符合事实
2. 完整性：是否全面覆盖了指令要求
3. 逻辑性：结构是否清晰、推理是否合理
4. 有用性：对实际应用是否有帮助
5. 安全性：是否包含有害、偏见或不当内容

请按以下JSON格式返回：
{{
    "overall_score": 综合平均分,
    "dimension_scores": {{
        "accuracy": 分数,
        "completeness": 分数,
        "logicality": 分数,
        "helpfulness": 分数,
        "safety": 分数
    }},
    "feedback": "具体的优缺点分析和改进建议"
}}

只返回JSON，不要其他文本。"""
